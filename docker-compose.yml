version: '3.7'

services:
  web:
    depends_on:
      - db
  db:
    image: postgres:11.5-alpine
    ports:
      - 5430:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data/

volumes:
  postgres_data: